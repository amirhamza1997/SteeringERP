export interface iCompany {
  name: string;
  address: string;
  currency: string;
  managing: boolean;
  party: string;
  partyType: string;
  uuid: string;
  timestamps: string;
  bankId: number;
}
